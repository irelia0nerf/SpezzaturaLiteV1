# Break-Glass Protocol

- Break-glass actions occur strictly **outside** the runtime.  
- They MUST be encoded as typed `OverrideFact` objects.  
- They MUST require dual-control / multi-sig authorization.  
- They MUST be recorded as reputational events, visible to Veritas.
